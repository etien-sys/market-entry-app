name = "seti-notion-proxy"
main = "index.js"
compatibility_date = "2024-09-23"

# Secrets are NOT stored here — set them with:
#   npx wrangler secret put NOTION_API_KEY
#   npx wrangler secret put NOTION_DB_ID
